/** Limite de charge utile d'une requête sur Vercel : ~4,5 Mo. On garde une marge. */
export const MAX_FILE_BYTES = 4 * 1024 * 1024;

/** Côté le plus long d'une photo après réduction dans le navigateur. */
export const MAX_IMAGE_SIDE = 1600;
